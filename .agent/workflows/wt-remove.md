---
description: Remove a Git worktree
---
1. Run the following command to remove a worktree:
```bash
git worktree remove ../worktrees/ProfeVision/<branch_name>
```
2. Optionally, delete the branch if it's no longer needed:
```bash
git branch -d <branch_name>
```
